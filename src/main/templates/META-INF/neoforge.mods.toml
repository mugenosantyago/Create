modLoader = "javafml"
# The loader version doesn't matter. Modify the NeoForge and/or Minecraft version ranges instead.
loaderVersion = "[0,)"
issueTrackerURL = "https://github.com/Creators-of-Create/Create/issues"
license = "${mod_license}"

[[mods]]
modId = "${mod_id}"
version = "${mod_version}"
displayName = "${mod_name}"
displayURL = "https://www.curseforge.com/minecraft/mc-mods/create"
updateJSONURL = "https://api.modrinth.com/updates/create/forge_updates.json?neoforge=only"
logoFile = "icon.png"
authors = "${mod_author}"
description = '''${mod_description}'''

[[mixins]]
config = "${mod_id}.mixins.json"

[[dependencies."${mod_id}"]]
modId = "neoforge"
type = "required"
versionRange = "[${neo_version},)"
ordering = "NONE"
side = "BOTH"

[[dependencies."${mod_id}"]]
modId = "minecraft"
type = "required"
versionRange = "[${minecraft_version}]"
ordering = "NONE"
side = "BOTH"

# TODO: Flywheel has no MC 1.21.8 build yet. Re-enable once ported.
# [[dependencies."${mod_id}"]]
# modId = "flywheel"
# type = "required"
# versionRange = "${flywheel_version_range}"
# ordering = "AFTER"
# side = "CLIENT"

# TODO: Ponder has no MC 1.21.8 build yet. Re-enable once ported.
# [[dependencies."${mod_id}"]]
# modId = "ponder"
# type = "required"
# versionRange = "[${ponder_version},)"
# ordering = "AFTER"
# side = "BOTH"

# Versions before 0.6.9 do not have sprite util under the api package
[[dependencies."${mod_id}"]]
modId = "sodium"
type = "optional"
reason = "Versions before 0.6.9 do not include the neccesary APIs Create needs to work properly with Sodium."
versionRange = "[0.6.9,)"
ordering = "NONE"
side = "CLIENT"

# Versions before 0.14.7 crash with level wrappers
[[dependencies."${mod_id}"]]
modId = "lithium"
type = "optional"
reason = "Versions before 0.14.7 crash when paired with Create, Update Lithium."
versionRange = "[0.14.7,)"
ordering = "NONE"
side = "BOTH"

# TODO: Update JourneyMap version requirement once 1.21.8 release is available
[[dependencies."${mod_id}"]]
modId = "journeymap"
type = "optional"
reason = "Versions before 1.21.8 builds do not contain the APIs Create requires to work with JourneyMap."
versionRange = "[1.21.8-6.0.0,)"
ordering = "NONE"
side = "CLIENT"

[[dependencies."${mod_id}"]]
modId = "xaeroworldmap"
type = "optional"
reason = "Versions before 1.40.2 do not contain the classes Create requires to work with Xaero's World Map."
versionRange = "[1.40.2,)"
ordering = "NONE"
side = "CLIENT"

# Incompatible Mods

# Users should use lithium instead, it's up to date and does not crash with level wrappers (anymore)
[[dependencies."${mod_id}"]]
modId = "radium"
type = "incompatible"
reason = "Radium is a unofficial port of Lithium, Lithium is now natively available for NeoForge and does not suffer from the same issues radium does, use Lithium instead."
ordering = "NONE"
side = "BOTH"

# Does not improve memory usage, results in connected textures not working as intended.
[[dependencies."${mod_id}"]]
modId = "palladium"
type = "incompatible"
reason = "Palladium does not improve memory usage by anything meaningful, and results in broken visuals for Create's blocks with connected textures."
ordering = "NONE"
side = "BOTH"
